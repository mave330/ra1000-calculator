# RA1000 Calculator

Calculates the exact geographic point an aircraft reaches 1000ft Radio Altitude on approach.

## Installation

```bash
pip install -e .
```

## Usage

```bash
ra1000 --original-csv <path-to-original-csv> --samples-csv <path-to-samples-csv> --target-ra 1000.0 --output results.csv
```
